# 项目交接文档（PROGRESS）

> 这份文档用于换电脑 / 换 Cursor 会话时无缝续接。新机器上让 Cursor 先读这份 + `docs/方案.md`，即可恢复上下文。
> 最后更新：2026-06-07

## 这是什么

一款 iOS 优先、面向**减肥/体重管理**人群的饮食运动热量打卡 App。

- **产品方案文档**：`docs/方案.md`（已锁定核心战略与边界，是唯一权威设计文档）。
- **当前仓库**：一个用 **React + Vite + TypeScript + Tailwind + Framer Motion** 做的**高保真交互 Web 原型**，用于可视化 UI/UX 与核心交互（不是最终 iOS 代码，最终用 SwiftUI 实现）。

## 如何运行（新电脑）

```bash
# 1. 装 Node.js（建议 18+，用官网安装包或 nvm，别用会编译源码的老 brew）
# 2. 进入 prototype 目录
cd prototype
# 3. 安装依赖
npm install
# 4. 启动开发服务器
npm run dev
# 浏览器打开 http://localhost:5173 （vite.config.ts 开了 host:true，可局域网用手机访问）
```

- `?static=1` URL 参数：关闭动画，用于无头截图（见 `src/anim.ts`）。

## 技术栈与目录

- `src/screens/`：三个一级主屏 — `HomeScreen`(今日) / `ProgressScreen`(趋势) / `MeScreen`(我的)；`DiaryScreen` 现作为二级记录详情页，不再作为一级 tab 渲染。
- `src/components/`：`PhoneFrame`(iPhone 外框) / `Card` / `CalorieRing` / `MacroBars` / `Heatmap` / `WeightTrend` / `IntakeTrend` / `TabBar` / `QuickAddSheet`(右侧悬浮 + 快速记录 sheet)。
- `src/data/mock.ts`：所有 mock 数据（range-aware：周/月/年；`daySummary(offset)` 生成历史某天数据）。
- `public/foods/`：真实食物图片（oatmeal/salad/bread/latte/avocado/salmon）。
- `tailwind.config.js`：自定义主题（canvas/surface/brand/status/macro 配色、圆角、shadow-float/shadow-card）。

## 已实现的关键交互（原型）

- **底部导航 + 右侧悬浮"+"快速记录**：底部一级 tab 收敛为 今日 / 趋势 / 我的，视觉上是类似趋势页 周/月/年 的小型 segmented 胶囊；选中态为内层小圆角，外层胶囊带阴影/毛玻璃。右侧独立悬浮圆形 `+` 是全局记录动作，不是 tab；`tab 胶囊 + +` 作为整体居中，尺寸和基线接近。圆形 + 按钮从右侧 morph 放大成 sheet（Container Transform），关闭反向收缩；medium/large 两档；拖动跟手、下滑关闭、上滑展开；顶部为关闭 + 日期/时间 + 饮食/运动圆角分段。medium 是快速启动器：餐次分段 + 轻量餐次建议 + 拍照识别/相册/扫码/搜索一行入口 + 常吃/最近；点搜索时按钮原地 morph 成搜索框并同步展开 large；无输入从 large 下滑回 medium 时搜索态复原。
- **一级 tab 切换动画**：保留上移动画手感，新页面从下方小幅上移淡入、旧页面小幅上移淡出；不要恢复 `AnimatePresence mode="wait"` 这类先卸载旧页再等待新页的模式，快速切 tab 会导致空白页。当前实现用同一容器内新旧页面短暂交接。
- **QuickAdd / 首页餐次逻辑**：后续正式实现需用 `inferDefaultMeal({ now, recordedMeals, lastMealTime })` 这类规则函数推断默认餐次，不只按时间。推荐与缺口提醒要分离：如午餐窗口已过但午餐未记，可 `recommendedMeal=加餐`、`missedMeal=午餐`，首页显示"午餐还没记录 / 先补午餐 · 有吃再记"，午餐按钮为补记，加餐按钮只标"有吃再记/可选"，不要写成"推荐加餐"。补记日期不显示今日餐次建议，按实际记录。餐次时间默认用系统时间窗，但首页快捷餐次提示、QuickAdd 餐次建议和"我的 → 记录偏好 → 餐次时间与提醒"都提供"调整餐次时间"入口；设置面板展示常规/晚起/轮班模板、三餐时间窗、用餐本地通知开关/提醒时间和保存校验规则。作息模板切换应同步覆盖三餐时间窗和提醒时间：常规=05:00–10:30/11:00–14:00/17:00–20:30，晚起=08:30–12:00/12:30–15:30/18:30–22:00，轮班原型=09:00–13:00/14:00–18:00/19:00–23:30 且提醒默认关。
- **今日页**：目标化热量环 + 蛋白质缺口 + 三大营养素 + 下一餐规则模板建议 + 今日活动/步数 + 活动下方快捷餐次入口 + 一行今日记录摘要入口。首页不常驻日期切换控件；支持左右滑轻量回看最近日期，回看时标题下显示"正在回看历史记录 · 回到今天"。
- **首次引导原型**：访问 `?onboarding=1` 可强制重走 onboarding；原型页顶部有"重走引导流程"按钮，`我的 → 其他 → 重新查看引导` 也可进入。无本地结果时默认进入引导。流程包含欢迎/经验分层/目标选择(减重、维持、增肌)/基础信息 mock/目标确认/每日预算/记录习惯。经验分层会影响后续提示：新手显示更明确解释，老手更快进入预算。完成后把 `selectedGoal` 等写入 `localStorage`，普通刷新保留选择。
- **目标驱动页面**：`App` 将 `selectedGoal` 传给 `HomeScreen` 和 `MeScreen`。首页热量环使用 `goalProfiles` 的 `ringMode`：减重=deficit、维持=band、增肌=goal-fill；首页 badge、下一餐建议、我的页目标预算区块随目标变化。
- **今日活动详情**：点击首页"今日活动"进入二级页，展示今日总活动消耗、来源明细（健康同步/手动记录）、HealthKit 读取说明、未同步/未授权降级状态；补录运动只保留轻入口"没有同步到运动？补录一项 ›"，不在详情页铺大表单。
- **趋势页**：range-aware（周/月/年）+ 内容区左右滑切换周期（周=上一/下一周，月=上一/下一月，年=上一/下一年）；标题下轻文本显示当前周期，偏移时显示"回到当前"。本期小结置顶 + 概览指标 + WeightTrend + IntakeTrend + Heatmap + 活动概览。定位是"看结果 + 找原因 + 下一步"，不是单纯图表集合。
  - `IntakeTrend` 支持 `offset` 和可访问柱状按钮：周/月选中看当天餐次与偏差解释，并提供"查看当天记录"入口；年视图选中看月度摘要与"查看月度记录"入口。
  - `Heatmap` 支持 `range + offset` 真实日期生成：月视图真实日历空白占位、年视图跨年尾格不可点，点格子展示具体日期 + 完整度 + 补记/查看入口。
  - `ProgressScreen` 切换 range 或周期 offset 时要重置图表选中态，避免残留上一个周期的详情。
- **记录 / 历史（二级页）**：一级 tab 已移除"日记"。`DiaryScreen` 改为二级记录详情页：顶部返回 + 日期/摘要 + 添加/补记入口，默认内容直接展示当天餐次/照片/来源/热量列表；空记录展示补记入口。入口来自首页一行记录摘要、趋势 Heatmap / IntakeTrend 下钻、活动详情/记录摘要、QuickAdd 保存后的 toast 等。相册能力以增强视图保留：当天记录有照片时右上角显示"照片 N ›"，月度记录页顶部提供"列表 / 照片"切换，照片视图展示本月照片墙/相册月历；不作为一级 tab 或当天默认页。记录详情支持左右滑：天记录按前一天/后一天切换并显示"回到今天"，月记录按上月/下月切换并显示"回到本月"，月度照片视图滑动后保持照片视图。
- **我的页**：目标与预算（含每日步数目标）等设置项。

## 关键产品决策（详见 docs/方案.md）

- v1 主打减肥/体重管理；iOS 优先；**只做海外区不上中国区，但支持中文用户**（双语 i18n + 中式食物数据）。
- 数据诚实：AI 只识别食物+估份量，营养值查库，结果可编辑、标来源+置信度。
- 食物数据栈：USDA + Open Food Facts 自建索引 + FatSecret Premier Free + 多模态 LLM 解析 + 中式种子集。
- 健康关注层**硬性合规边界**：只做追踪+科普+免责，不诊断/不治疗/不预测医学数值。
- **goalProfile 目标驱动呈现层**：同一套卡片骨架，热量环三种模式（deficit 减重 / goal-fill 增重增肌 / band 维持改善膳食），靠配置切换不新建页面。v1 倾向收紧到减重+维持，其余轻量适配。
- 启动/首次引导必须做目标速度选择：当前体重 + 目标体重 → 推荐周期/速度档位（放松/稳定/积极/高强度）→ 生成每日预算和首页 badge。`减重中 · 每周 -0.5kg` 不能写死，来自 `targetPaceKgPerWeek`；用户输入"1 个月减/加 5kg"这类目标时需换算周速度并做安全护栏提示。
- 首页只显示当前主目标，不放减重/维持/增肌切换；BMR/TDEE/热量缺口等推导放 `ⓘ` 说明弹层，不常驻首页。
- 首页下一步建议走规则模板：按目标、下一餐、剩余热量、蛋白质缺口生成；AI 最多润色，不自由生成健康/运动建议。
- HealthKit 活动策略：优先用 `activeEnergyBurned` 作为总活动消耗；Workout/步数用于解释来源，不重复相加；无权限时显示未同步状态并允许手动运动补录。
- 右侧悬浮"+"是统一记录入口，默认饮食，运动作为 sheet 内次级模式（类型 + 时长 + 强度 → 估算消耗），不另开一级 tab；可从日期入口补昨天/前天/自选日期的用餐与运动记录。
- 信息架构最新定向：底部收敛为 今日 / 趋势 / 我的 三个一级 tab + 右侧悬浮 `+`；"日记/历史记录"降为二级页，不再替换成其他一级 tab。

## 已知问题 / 待办

- `QuickAddSheet` 关闭动画的颜色过渡、浮起阴影手感前期反复调过，若仍觉生硬可继续微调 `closeSpring` 阻尼与 `boxShadow`。
- 无头 Chrome 截图对动画元素偶发卡住/空白，用 `?static=1` 规避。
- 下一阶段（按方案）：信息架构 + 关键页面线框（Figma）→ 评审 → SwiftUI 实现。

## 方案文档里的下一步

确认战略后：先做信息架构 + 关键页面线框（Figma）→ 评审 → 再进入 SwiftUI 实现。本阶段不写正式代码。
